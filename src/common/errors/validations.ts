/**
 * ValidationErrorMessage
 * will hold the error message for the validation errors that occurred during the validation process
 */
export const ValidationErrorMessage = {
  isNumber: {
    en: 'Number must be a number',
    ar: ' يجب ان يكون رقم',
  },
};
